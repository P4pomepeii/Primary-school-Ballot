langgraph>=0.2
langchain-core>=0.3
pydantic>=2
fastapi>=0.115
uvicorn>=0.30
pytest>=8

# Install whichever of these match MODEL_PROVIDER you actually use:
# langchain-groq>=0.2      # MODEL_PROVIDER=groq
# langchain-aws>=0.2       # MODEL_PROVIDER=bedrock
